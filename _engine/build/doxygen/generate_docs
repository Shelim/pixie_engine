#!/bin/bash

cd "$(dirname "$0")"
cd ../../_build
rm -rf doxygen
mkdir doxygen

cd ..
rm -rf docs
mkdir docs

cd build/doxygen/unix

platform="$(uname -s)"

case "${platform}" in
    Darwin*) ./../../../depedency/doxygen/os_x/Doxygen doxyfile
	*) 
esac


